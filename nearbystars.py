

import cPickle as cP
from universal import *

HippDict = cP.load(open(picklePath+'hipp07.pickle','rb'))

class system

    class star

        def __init__(self):

            # essential
            self.dist = 
            self.bvcolor =
            #self.err_bvcolor
            self.logg
            self.err_logg
            self.mass
            self.err_mass
            self.log_radius
            self.err_log_radius
            self.bc
            self.err_bc
            self.teff
            self.err_logteff
            
            # other
            self.ra
            self.dec

    class planet

        def __init__(self,rp,alf,r):

            self.rp =
            self.alf =
            self.r = 
    
    class other

        def __init__(self):
            self.tauz = 
            self.tauez =

out = {mass:mass,z:z,age:age,lbol:10.0d0^o_logL,teff:10.0d0^o_logTe,logg:o_logG,$
            rad:(((const.gravc*mass*const.msun)/(10.0d0^o_logG))^0.5d0)/const.radsun,Vmag:$
            o_V,Bmag:o_B,mbol:o_mbol}